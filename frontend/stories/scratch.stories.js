import React from 'react'
import { storiesOf } from '@storybook/react'

storiesOf('Scratch', module).add('Explanation', () => (
  <p>This is just to flesh out any new components. Nothing here is final.</p>
))
